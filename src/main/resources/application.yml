spring:
  application:
    name: easymonitor

  datasource:
    url: jdbc:h2:mem:easymonitordb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: smtp.gmail.com
    port: 587
    username: ynexus22@gmail.com
    password: fhbh leri wlkm nxne
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp


  rabbitmq:
    addresses: amqp://Pluris:plu5@ft#2014@localhost:5672
